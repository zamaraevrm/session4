<Window x:Class="session4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:session4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        

        <ListBox Grid.Row="1" d:ItemsSource="{d:SampleData}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Height="200" Width="769" >
                        <Grid.Resources >
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="16"/>
                            </Style>
                        </Grid.Resources>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="46*"/>
                            <ColumnDefinition Width="81*"/>
                            <ColumnDefinition Width="33*"/>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" Source="/images/picture.png"/>

                        <StackPanel Grid.Column="1">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="10"/>
                                    <Setter Property="FontSize" Value="16"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Text" Value="sakjdfl"/>
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock />
                            <TextBlock/>
                            <TextBlock/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <StackPanel.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Margin" Value="10"/>
                                    <Setter Property="FontSize" Value="16"/>
                                    <Setter Property="Text" Value="sakjdfl"/>
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock />
                            <TextBlock/>
                            <TextBlock />
                        </StackPanel>

                        <TextBlock Text="dksljf" Grid.Column="2" TextAlignment="Right" HorizontalAlignment="Right" Margin="0,20,10,60" Width="127" TextWrapping="Wrap"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
    </Grid>
</Window>
